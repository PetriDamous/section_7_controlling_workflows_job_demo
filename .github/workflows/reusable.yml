name: Reusable Deploy
on: workflow_call # workflow_call allows this workflow to be called in other workflows
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: echo "Deploying & uploading..."
